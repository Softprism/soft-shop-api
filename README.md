# Soft-Shop API

API for the Soft-Shop service.

## Changelogs
#### v1.0.1
Fixed: Send correct price in new order email to users.
Refactored: prices in order models are no longer required
Refactored: calculations for subtotal, order fee and totalPrice fees are now done programmatically
Refactored: Baskets items are now individual items, existing products will create new entry

### Branches

- Feature Branch => Only **New** features should be committed to this branch.
- Hotfix Branch => Only **Fixes** should be committed to this branch (fixes for feature branch).
- **Main** Branch => Pull requests should be sent here.
